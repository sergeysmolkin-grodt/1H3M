[DEFAULT]
ConnectionType=initiator
ReconnectInterval=60
FileStorePath=../fix_store
FileLogPath=../fix_logs
StartTime=00:00:00
EndTime=00:00:00
UseDataDictionary=Y
DataDictionary=../fix_config/FIX44.xml # Укажите правильный путь, если он другой
HeartBtInt=30
# SocketUseSSL=N # Для plain text порта 5202
# Для SSL порта 5212, раскомментируйте SocketUseSSL=Y и могут понадобиться доп. параметры SSL

[SESSION]
# Session specific settings
BeginString=FIX.4.4
SenderCompID=demo.ftmo.7378494
TargetCompID=cServer
SocketConnectHost=demo-uk-eqx-01.p.c-trader.com
SocketConnectPort=5202 # Используем Plain text порт для начала
# Logon fields
# Username=your_username # Если требуется отдельное поле Username для Logon
Password=YOUR_CTRADER_ACCOUNT_PASSWORD # ЗАМЕНИТЕ НА ВАШ РЕАЛЬНЫЙ ПАРОЛЬ
SenderSubID=TRADE
# ResetSeqNumFlag=Y # Установить в Y для сброса номеров сообщений при каждом входе (для отладки)
# CheckLatency=N
# MaxLatency=120
# SendRedundantResendRequests=N
# ResendRequestChunkSize=0

# --- ВАЖНО ---
# 1. Замените YOUR_CTRADER_ACCOUNT_PASSWORD на ваш реальный пароль от счета 7378494.
# 2. Убедитесь, что путь к DataDictionary (FIX44.xml) корректен. 
#    Возможно, вам придется скачать FIX44.xml и поместить его в папку fix_config.
#    Или если quickfix установлен, он может быть в другом месте.
# 3. Создайте папки fix_store и fix_logs в корне вашего проекта (на одном уровне с fix_config). 